[phases.setup]
nixPkgs = ["...", "php84"]

[phases.build]
cmds = ["composer install --no-dev --no-scripts --no-interaction", "npm install", "npm run build", "php artisan config:cache", "php artisan route:cache"]

[start]
cmd = "php artisan serve --host 0.0.0.0 --port ${PORT}"